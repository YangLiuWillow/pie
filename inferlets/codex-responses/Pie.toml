[package]
name = "codex-responses"
version = "0.1.0"
description = "Codex Responses-API server with content-addressed KV session reuse"
authors = ["Yang Liu <liu@liszt.ai>"]

[runtime]
core = "^0.2.0"
mcp = "^0.2.0"

# No CLI parameters - this is an HTTP server that receives input via HTTP requests
[parameters]
