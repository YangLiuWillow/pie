{"id": "shell-create", "prompt": "Use the run_shell_command tool to create a file named greeting.txt containing exactly the text: hello from the benchmark. Then verify it with cat and stop.", "setup": "true", "check": "grep -q 'hello from the benchmark' greeting.txt"}
{"id": "fix-off-by-one", "prompt": "The file summing.py has a bug: total_upto(n) should sum the integers 1..n inclusive but it is off by one. Read the file, fix the bug with the edit tool, then run python3 -c 'from summing import total_upto; print(total_upto(10))' with the run_shell_command tool to confirm it prints 55, and stop.", "setup": "printf 'def total_upto(n):\\n    return sum(range(1, n))\\n' > summing.py", "check": "python3 -c 'from summing import total_upto; import sys; sys.exit(0 if total_upto(10)==55 else 1)'"}
{"id": "grep-count", "prompt": "Count how many lines in words.txt contain the substring 'ana' using the run_shell_command tool, write just that number to count.txt, verify with cat, and stop.", "setup": "printf 'banana\\napple\\nbandana\\ncherry\\nanagram\\ngrape\\n' > words.txt", "check": "test \"$(cat count.txt | tr -d '[:space:]')\" = 3"}
{"id": "add-function", "prompt": "Add a function median(xs) to stats.py that returns the median of a non-empty list of numbers (average the two middle values for even lengths). Use the edit tool, then run python3 -c 'from stats import median; print(median([1,3,2]), median([1,2,3,4]))' with run_shell_command to confirm it prints 2 2.5, and stop.", "setup": "printf 'def mean(xs):\\n    return sum(xs) / len(xs)\\n' > stats.py", "check": "python3 -c 'from stats import median; import sys; sys.exit(0 if median([1,3,2])==2 and median([1,2,3,4])==2.5 else 1)'"}
{"id": "rename-refactor", "prompt": "In app.py, rename the function fetch_data to load_records everywhere (definition and both call sites) using the edit tool. Then run python3 app.py with run_shell_command to confirm it still prints ok twice, and stop.", "setup": "printf 'def fetch_data(tag):\\n    return \"ok\"\\n\\nprint(fetch_data(\"a\"))\\nprint(fetch_data(\"b\"))\\n' > app.py", "check": "python3 app.py | grep -c ok | grep -q 2 && ! grep -q fetch_data app.py"}
